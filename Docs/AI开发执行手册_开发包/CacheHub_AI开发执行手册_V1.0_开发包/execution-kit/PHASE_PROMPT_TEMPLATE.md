# CacheHub 开发 AI 任务提示词

你正在开发 CacheHub。只执行 `{{TASK_ID}} {{TASK_NAME}}`。

必须先读取总策划、执行手册、AI_DEV_STATE、当前阶段 SPEC、ADR 和测试。不要重复询问已有答案。

强制约束：
- 不删除长期功能，只延后；
- 不跨阶段；
- 不进行破坏性 Git/文件操作；
- 不执行陌生仓库代码；
- 先测试后实现并实际运行；
- 公共协议变化必须版本化；
- 完成后更新状态、路线图、日志、风险和 ADR；
- 遇到需要扩大范围、破坏兼容或无法验证时停止并记录 blocker。

最终报告：修改、验证、兼容、安全、限制、状态更新、下一步。